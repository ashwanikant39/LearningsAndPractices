@model BEEKP.Areas.Admin.Models.AdminClusterViewModel

@if (Model.ListClusters != null)
{
    <table class="table table-striped table-hover table-bordered">

        <thead>
            <tr>
                <th>
                    Sector Name
                </th>
                <th>
                    Cluster Name
                </th>
               
                <th>
                    Craeted Date
                </th>

                <th align="center" valign="middle">
                    Action
                </th>
            </tr>
        </thead>
        <tbody>

            @for (Int32 i = 0; i < Model.ListClusters.Count; i++)
            {

            <tr>
                
                <td>
                    <label>@Model.ListClusters[i].SectorName</label>
                </td>
                <td>
                    <label>@Model.ListClusters[i].ClusterName </label>
                </td>
                <td>
                    <label>@Model.ListClusters[i].CreatedDate </label>
                </td>
                <td align="center" valign="middle">
                    @*@if (Model.PagePermission.role_edit == BEEKP.Class.Constants.PAGE_PERMISSION_YES){}*@
                    <a href="@Url.Action("ClusterAddEdit", "AdminCluster", new { _id = BEEKP.Class.Encryption.Encrypt(Model.ListClusters[i].Id.ToString(), true) })" title="" class="custom-edit-tooltip"><i class="fa fa-pencil"></i></a>

                    @*@if (Model.PagePermission.role_delete == BEEKP.Class.Constants.PAGE_PERMISSION_YES)
            {
                @Html.Raw("|");
                <a href="@Url.Action("DeleteNews", "AdminNews", new { sNewsID = BEEKP.Class.Encryption.Encrypt(Model.ListClusters[i].Id.ToString(),true)})" title="" data-toggle="confirmation" data-original-title="Are you sure ?"><i class="fa fa-trash-o"></i></a>
            }*@
                </td>
            </tr>
            }
        </tbody>
    </table>
}

